dataset_id: "main_pdfs"

pipeline_version: "v0.4.0"

embedding:
  model: "sentence-transformers/all-MiniLM-L6-v2"
  batch_size: 32
  normalize: true

chunking:
  chunk_chars: 1200
  overlap: 200
  max_chunks_per_doc: 0

ingest:
  max_files: 50
  only_match: ""
  upsert_batch_size: 64
